import { Button } from 'antd';

export default () => {
  return (
    <div>
      <span className="w-full flex flex-row justify-center pb-16">sdfd</span>
      <Button>只有 Admin 可以看到这个按钮</Button>
    </div>
  );
};
